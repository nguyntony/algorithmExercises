// 5. A Pythagorean triplet is a set of three natural numbers, a < b < c, for which, a^2 + b^2 = c^2
// For example, 3^2 + 4^2 = 5^2, 9 + 16 = 25.
// There exists exactly one Pythagorean triplet for which a + b + c = 1000. Find a, b, and c.
// a, b, and c are unique numbers. a != b != c.

const findTriple = () => {
    let a, b, c
    const max = Math.round(Math.sqrt(1000))
    console.log(max)

}

findTriple()